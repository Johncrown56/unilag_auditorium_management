import React from "react";
import Breadcrumb from "../../components/Breadcrumb";

type Props = {};

const Notications = (props: Props) => {
  return (
    <div className="mx-auto max-w-270">
      <Breadcrumb pageName="Notications" />
    </div>
  );
};

export default Notications;
